{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Launch React App",
          "type": "chrome",
          "request": "launch",
          "preLaunchTask": "npm start",
          "url": "http://localhost:3000",
          "webRoot": "${workspaceFolder}/src"
      }
  ]
}
